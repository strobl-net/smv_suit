class Organisation {
  final int id;
  final String name;
  final String description;
  final String location;
  final DateTime added;
  final DateTime changed;

  Organisation(this.id, this.name, this.description, this.location, this.added, this.changed);
}